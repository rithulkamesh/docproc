# API base URL for the docproc backend (Go). Must be a full URL.
# For local dev, start the API (e.g. from repo root: run the Go server on port 8080).
# Default if unset: http://localhost:8080
VITE_DOCPROC_API_URL=http://localhost:8080
